import Section from "./common/Section";
import Title from "./common/Title";

export default function Projects() {
  //TODO: Field the component when projects are publish on github.
  return (
    <Section id="projects">
      <Title text={"Proyectos"} style="place-self-start" />
      <div className="h-screen"></div>
    </Section>
  );
}
